<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- No Title Bar. -->
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TabTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>                    <!-- Tab Backgorund Color-->
        <item name="android:textColorPrimary">@android:color/white</item>       <!-- selected tab text -->
        <item name="android:textColorSecondary">#afafaf</item>                  <!-- not selected tab text -->
        <item name="colorAccent">@android:color/white</item>                    <!-- selected tab underline-->
    </style>

    <style name="CustomTextAppearanceTab" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="CustomDialog" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="windowNoTitle">true</item>
    </style>
</resources>
